---
- name: Reload systemd daemon
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart kafka
  ansible.builtin.systemd:
    name: kafka
    state: restarted
    daemon_reload: true
  when: kafka_service_installed | default(false)
